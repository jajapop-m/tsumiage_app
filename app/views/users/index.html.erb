<%= paginate @users %>

<ul class="users">
  <% @users.each do |user| %>
    <li>
      <%= link_to user.name, user ,'data-toggle': "modal", 'data-target': "#userModal" %>
    </li>
  <% end %>
</ul>

<%= paginate @users %>